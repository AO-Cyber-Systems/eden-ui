<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Eden UI — <%= @block.name %> Preview</title>
  <%= eden_design_font_tags %>
  <%= eden_design_theme_script %>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          fontFamily: {
            display: ['Outfit', 'system-ui', 'sans-serif'],
            body: ['Plus Jakarta Sans', 'system-ui', 'sans-serif'],
            mono: ['JetBrains Mono', 'Fira Code', 'monospace'],
          },
          colors: {
            gold: {
              50: '#fdf8ef', 100: '#faecd5', 200: '#f4d5aa', 300: '#edb974',
              400: '#e59a3c', 500: '#d4a853', 600: '#c49545', 700: '#a67a38',
              800: '#856131', 900: '#6c5029', 950: '#3d2a14',
            },
          },
        },
      },
    }
  </script>
  <style>
    <%= styleguide_tokens_css %>
    <%= styleguide_animations_css %>
  </style>
</head>
<body class="bg-white dark:bg-zinc-950 text-zinc-900 dark:text-zinc-100 font-body antialiased">
  <% partial_path = "eden_ui/styleguide/blocks/#{@block_category_slug}/#{@block.slug}" %>
  <% if lookup_context.exists?(partial_path, [], true) %>
    <%= render partial: partial_path %>
  <% else %>
    <div class="flex items-center justify-center min-h-screen">
      <p class="text-zinc-400">Block preview not yet implemented for "<%= @block.slug %>"</p>
    </div>
  <% end %>
</body>
</html>
