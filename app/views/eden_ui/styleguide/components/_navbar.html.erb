<%= styleguide_example(
  title: "Default Navbar",
  description: "Standard navbar with navigation links",
  source: 'eden_navbar(fixed: false, app_name: "Eden UI") do
  content_tag(:ul, class: "flex flex-col md:flex-row md:space-x-8 font-medium") do
    safe_join([
      content_tag(:li, link_to("Home", "#", class: "block py-2 px-3 text-blue-700 dark:text-blue-500")),
      content_tag(:li, link_to("About", "#", class: "block py-2 px-3 text-gray-900 hover:text-blue-700 dark:text-white dark:hover:text-blue-500")),
      content_tag(:li, link_to("Services", "#", class: "block py-2 px-3 text-gray-900 hover:text-blue-700 dark:text-white dark:hover:text-blue-500")),
      content_tag(:li, link_to("Contact", "#", class: "block py-2 px-3 text-gray-900 hover:text-blue-700 dark:text-white dark:hover:text-blue-500"))
    ])
  end
end'
) do %>
  <div class="w-full border border-zinc-200 dark:border-zinc-700 rounded-lg overflow-hidden">
    <%= eden_navbar(fixed: false) do %>
      <ul class="flex flex-col md:flex-row md:space-x-8 font-medium">
        <li><a href="#" class="block py-2 px-3 text-blue-700 dark:text-blue-500">Home</a></li>
        <li><a href="#" class="block py-2 px-3 text-gray-900 hover:text-blue-700 dark:text-white dark:hover:text-blue-500">About</a></li>
        <li><a href="#" class="block py-2 px-3 text-gray-900 hover:text-blue-700 dark:text-white dark:hover:text-blue-500">Services</a></li>
        <li><a href="#" class="block py-2 px-3 text-gray-900 hover:text-blue-700 dark:text-white dark:hover:text-blue-500">Contact</a></li>
      </ul>
    <% end %>
  </div>
<% end %>

<%= styleguide_example(
  title: "Navbar with Brand",
  description: "Navbar displaying an app name with bordered style",
  source: 'eden_navbar(fixed: false, bordered: true, app_name: "Eden UI") do
  content_tag(:ul, class: "flex flex-col md:flex-row md:space-x-8 font-medium") do
    safe_join([
      content_tag(:li, link_to("Dashboard", "#", class: "block py-2 px-3 text-gray-900 dark:text-white")),
      content_tag(:li, link_to("Settings", "#", class: "block py-2 px-3 text-gray-900 dark:text-white"))
    ])
  end
end'
) do %>
  <div class="w-full border border-zinc-200 dark:border-zinc-700 rounded-lg overflow-hidden">
    <nav class="bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700">
      <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
        <a href="#" class="flex items-center space-x-3 rtl:space-x-reverse">
          <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">Eden UI</span>
        </a>
        <div class="hidden w-full md:block md:w-auto">
          <ul class="flex flex-col md:flex-row md:space-x-8 font-medium">
            <li><a href="#" class="block py-2 px-3 text-gray-900 hover:text-blue-700 dark:text-white dark:hover:text-blue-500">Dashboard</a></li>
            <li><a href="#" class="block py-2 px-3 text-gray-900 hover:text-blue-700 dark:text-white dark:hover:text-blue-500">Settings</a></li>
          </ul>
        </div>
      </div>
    </nav>
  </div>
<% end %>

<%= styleguide_example(
  title: "Rounded Navbar",
  description: "Navbar with rounded corners",
  source: 'eden_navbar(fixed: false, rounded: true, bordered: true) do
  # navigation content
end'
) do %>
  <div class="w-full p-2">
    <nav class="bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700 rounded-lg">
      <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
        <a href="#" class="flex items-center space-x-3 rtl:space-x-reverse">
          <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">My App</span>
        </a>
        <div class="hidden w-full md:block md:w-auto">
          <ul class="flex flex-col md:flex-row md:space-x-8 font-medium">
            <li><a href="#" class="block py-2 px-3 text-blue-700 dark:text-blue-500">Home</a></li>
            <li><a href="#" class="block py-2 px-3 text-gray-900 hover:text-blue-700 dark:text-white dark:hover:text-blue-500">Projects</a></li>
            <li><a href="#" class="block py-2 px-3 text-gray-900 hover:text-blue-700 dark:text-white dark:hover:text-blue-500">Team</a></li>
          </ul>
        </div>
      </div>
    </nav>
  </div>
<% end %>
