<%
  id ||= "crud-modal"
  title ||= "Create"
  size ||= :md
  submit_text ||= "Save"
  cancel_text ||= "Cancel"
  form_url ||= "#"
  form_method ||= :post
  html_options ||= {}
  content ||= nil
%>

<%= eden_modal(id: id, size: size, **html_options) do %>
  <form action="<%= form_url %>" method="<%= form_method == :get ? 'get' : 'post' %>">
    <% unless form_method == :get || form_method == :post %>
      <input type="hidden" name="_method" value="<%= form_method %>">
    <% end %>

    <div class="space-y-4 mb-6">
      <%= content %>
    </div>

    <div class="flex items-center gap-3 border-t border-zinc-200 dark:border-zinc-600 pt-4">
      <%= eden_button(variant: :primary, type: :submit) { submit_text } %>
      <button type="button" data-action="eden-modal#close" class="text-zinc-500 bg-white hover:bg-zinc-100 focus:ring-4 focus:ring-zinc-200 rounded-lg border border-zinc-200 text-sm font-medium px-5 py-2.5 hover:text-zinc-900 dark:bg-zinc-700 dark:text-zinc-300 dark:border-zinc-500 dark:hover:text-white dark:hover:bg-zinc-600 dark:focus:ring-zinc-600"><%= cancel_text %></button>
    </div>
  </form>
<% end %>
