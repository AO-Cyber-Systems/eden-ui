<%
  bordered ||= true
  html_options ||= {}
  content ||= nil

  border_class = bordered ? "border border-zinc-200 dark:border-zinc-600" : ""
  classes = ["w-full text-sm font-medium text-zinc-900 bg-white rounded-lg dark:bg-zinc-700 dark:text-white #{border_class}", html_options.delete(:class)].compact_blank.join(" ")
%>

<ul class="<%= classes %>" <%= tag.attributes(html_options) %>>
  <%= content %>
</ul>
