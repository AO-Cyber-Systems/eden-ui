<%
  title ||= nil
  subtitle ||= nil
  dark_bg ||= false
  gradient ||= false
  html_options ||= {}
  content ||= nil

  bg_classes = if gradient
    "bg-gradient-to-r from-primary-500 to-purple-600 text-white"
  elsif dark_bg
    "bg-zinc-900 text-white"
  else
    "bg-white dark:bg-zinc-900"
  end

  classes = [bg_classes, html_options.delete(:class)].compact_blank.join(" ")
%>

<section class="<%= classes %>" <%= tag.attributes(html_options) %>>
  <div class="py-8 px-4 mx-auto max-w-screen-xl text-center lg:py-16">
    <% if title %>
      <h1 class="mb-4 text-4xl font-extrabold tracking-tight leading-none md:text-5xl lg:text-6xl <%= gradient || dark_bg ? "text-white" : "text-zinc-900 dark:text-white" %>"><%= title %></h1>
    <% end %>
    <% if subtitle %>
      <p class="mb-8 text-lg font-normal lg:text-xl sm:px-16 lg:px-48 <%= gradient || dark_bg ? "text-zinc-200" : "text-zinc-500 dark:text-zinc-400" %>"><%= subtitle %></p>
    <% end %>
    <%= content %>
  </div>
</section>
