<%
  name ||= nil
  id ||= name
  value ||= nil
  placeholder ||= nil
  rows ||= 4
  disabled ||= false
  required ||= false
  error ||= nil
  html_options ||= {}
  content ||= nil

  state_classes = if error
    "bg-red-50 border-red-500 text-red-900 placeholder-red-700 focus:ring-red-500 focus:border-red-500 dark:text-red-500 dark:placeholder-red-500 dark:border-red-500"
  else
    "bg-zinc-50 border-zinc-300 text-zinc-900 focus:ring-primary-500 focus:border-primary-500 dark:bg-zinc-700 dark:border-zinc-600 dark:placeholder-zinc-400 dark:text-white dark:focus:ring-primary-500 dark:focus:border-primary-500"
  end

  classes = ["block p-2.5 w-full text-sm rounded-lg border", state_classes, html_options.delete(:class)].compact_blank.join(" ")
%>

<textarea <% if name %>name="<%= name %>"<% end %> <% if id %>id="<%= id %>"<% end %> rows="<%= rows %>" <% if placeholder %>placeholder="<%= placeholder %>"<% end %> class="<%= classes %>" <%= "disabled" if disabled %> <%= "required" if required %> <%= tag.attributes(html_options) %>><%= value || content %></textarea>
