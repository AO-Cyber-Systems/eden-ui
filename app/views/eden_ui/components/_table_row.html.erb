<%
  html_options ||= {}
  content ||= nil

  classes = ["bg-white border-b dark:bg-zinc-800 dark:border-zinc-700", html_options.delete(:class)].compact_blank.join(" ")
%>

<tr class="<%= classes %>" <%= tag.attributes(html_options) %>>
  <%= content %>
</tr>
