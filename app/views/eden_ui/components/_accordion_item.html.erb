<%
  title ||= nil
  open ||= false
  id ||= "accordion-item-#{title&.parameterize}"
  html_options ||= {}
  content ||= nil
%>

<div data-controller="eden-accordion" <%= tag.attributes(html_options) %>>
  <h2>
    <button type="button" class="flex items-center justify-between w-full p-5 font-medium rtl:text-right text-gray-500 border-b border-gray-200 dark:border-gray-700 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800 gap-3 <%= open ? "bg-gray-100 dark:bg-gray-800 text-gray-900 dark:text-white" : "" %>" data-action="eden-accordion#toggle" aria-expanded="<%= open %>">
      <span><%= title %></span>
      <svg class="w-3 h-3 shrink-0 <%= open ? "rotate-180" : "" %> transition-transform" data-eden-accordion-target="icon" fill="none" viewBox="0 0 10 6"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5 5 1 1 5"/></svg>
    </button>
  </h2>
  <div data-eden-accordion-target="body" class="<%= open ? "" : "hidden" %>">
    <div class="p-5 border-b border-gray-200 dark:border-gray-700 dark:bg-gray-900">
      <p class="text-gray-500 dark:text-gray-400"><%= content %></p>
    </div>
  </div>
</div>
