<%
  prepend ||= nil
  append ||= nil
  html_options ||= {}
  content ||= nil
%>

<div class="flex" <%= tag.attributes(html_options) %>>
  <% if prepend %>
    <span class="inline-flex items-center px-3 text-sm text-zinc-900 bg-zinc-200 border border-e-0 border-zinc-300 rounded-s-md dark:bg-zinc-600 dark:text-zinc-400 dark:border-zinc-600"><%= prepend %></span>
  <% end %>
  <div class="<%= prepend && append ? '' : prepend ? 'rounded-e-lg' : append ? 'rounded-s-lg' : 'rounded-lg' %> flex-1">
    <%= content %>
  </div>
  <% if append %>
    <span class="inline-flex items-center px-3 text-sm text-zinc-900 bg-zinc-200 border border-s-0 border-zinc-300 rounded-e-md dark:bg-zinc-600 dark:text-zinc-400 dark:border-zinc-600"><%= append %></span>
  <% end %>
</div>
