<%
  html_options ||= {}
  content ||= nil

  classes = ["px-2 py-1.5 text-xs font-semibold text-zinc-800 bg-zinc-100 border border-zinc-200 rounded-lg dark:bg-zinc-600 dark:text-zinc-100 dark:border-zinc-500", html_options.delete(:class)].compact_blank.join(" ")
%>

<kbd class="<%= classes %>" <%= tag.attributes(html_options) %>><%= content %></kbd>
