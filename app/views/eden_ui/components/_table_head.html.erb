<%
  columns ||= []
  html_options ||= {}
  content ||= nil

  classes = ["text-xs text-zinc-700 uppercase bg-zinc-50 dark:bg-zinc-700 dark:text-zinc-400", html_options.delete(:class)].compact_blank.join(" ")
%>

<thead class="<%= classes %>" <%= tag.attributes(html_options) %>>
  <tr>
    <% if columns.any? %>
      <% columns.each do |col| %>
        <th scope="col" class="px-6 py-3"><%= col %></th>
      <% end %>
    <% else %>
      <%= content %>
    <% end %>
  </tr>
</thead>
