<%
  title ||= "Under Maintenance"
  description ||= "We're working on improvements. We'll be back shortly."
  estimated_time ||= nil
  notify_url ||= nil
  notify_placeholder ||= "Enter your email"
  html_options ||= {}
  content ||= nil

  classes = [
    "flex min-h-[60vh] flex-col items-center justify-center px-4 py-8 text-center",
    html_options.delete(:class)
  ].compact_blank.join(" ")
%>

<div class="<%= classes %>" <%= tag.attributes(html_options) %>>
  <%# Wrench/cog icon %>
  <div class="mb-6">
    <svg class="mx-auto h-24 w-24 text-gray-400 dark:text-gray-500" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
      <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 13v-2a1 1 0 0 0-1-1h-.8l-.7-1.7.6-.5a1 1 0 0 0 0-1.4l-1.5-1.5a1 1 0 0 0-1.4 0l-.5.6L14 5.8V5a1 1 0 0 0-1-1h-2a1 1 0 0 0-1 1v.8l-1.7.7-.5-.6a1 1 0 0 0-1.4 0L4.9 7.4a1 1 0 0 0 0 1.4l.6.5-.7 1.7H4a1 1 0 0 0-1 1v2a1 1 0 0 0 1 1h.8l.7 1.7-.6.5a1 1 0 0 0 0 1.4l1.5 1.5a1 1 0 0 0 1.4 0l.5-.6 1.7.7v.8a1 1 0 0 0 1 1h2a1 1 0 0 0 1-1v-.8l1.7-.7.5.6a1 1 0 0 0 1.4 0l1.5-1.5a1 1 0 0 0 0-1.4l-.6-.5.7-1.7h.8a1 1 0 0 0 1-1Zm-9 3a3 3 0 1 1 0-6 3 3 0 0 1 0 6Z"/>
    </svg>
  </div>

  <h1 class="mb-4 text-3xl font-bold tracking-tight text-gray-900 dark:text-white md:text-4xl"><%= title %></h1>
  <p class="mb-6 text-lg text-gray-500 dark:text-gray-400 max-w-md"><%= description %></p>

  <% if estimated_time %>
    <div class="mb-6">
      <span class="inline-flex items-center gap-1.5 rounded-full bg-yellow-100 px-3 py-1.5 text-sm font-medium text-yellow-800 dark:bg-yellow-900 dark:text-yellow-300">
        <svg class="h-4 w-4" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
          <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z"/>
        </svg>
        Estimated downtime: ~<%= estimated_time %>
      </span>
    </div>
  <% end %>

  <% if notify_url %>
    <div class="mb-6 w-full max-w-sm">
      <p class="mb-3 text-sm text-gray-500 dark:text-gray-400">Get notified when we're back online:</p>
      <form action="<%= notify_url %>" method="post" class="flex gap-2">
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <input
          type="email"
          name="email"
          placeholder="<%= notify_placeholder %>"
          required
          class="flex-1 rounded-lg border border-gray-300 bg-gray-50 px-4 py-2.5 text-sm text-gray-900 focus:border-primary-500 focus:ring-primary-500 dark:border-gray-600 dark:bg-gray-700 dark:text-white dark:placeholder-gray-400 dark:focus:border-primary-500 dark:focus:ring-primary-500"
        >
        <%= eden_button(variant: :primary, type: :submit) { "Notify me" } %>
      </form>
    </div>
  <% end %>

  <% if content %>
    <div class="mt-4 w-full max-w-lg">
      <%= content %>
    </div>
  <% end %>
</div>
